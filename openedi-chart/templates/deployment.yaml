apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ include "openedi.fullname" . }}
  labels:
    app: {{ include "openedi.name" . }}
spec:
  replicas: {{ .Values.replicaCount }}
  selector:
    matchLabels:
      app: {{ include "openedi.name" . }}
  template:
    metadata:
      labels:
        app: {{ include "openedi.name" . }}
    spec:
      containers:
        - name: openedi
          image: "{{ .Values.image.repository }}:{{ .Values.image.tag }}"
          ports:
            - containerPort: 80
